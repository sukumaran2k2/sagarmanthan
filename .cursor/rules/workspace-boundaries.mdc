---
description: Restrict edits to active workspace only
alwaysApply: true
---

# Workspace Boundaries

- Treat `D:\sagarmanthan` as the only writable codebase for this session.
- Do not create, modify, delete, or run refactors in:
  - `D:\sagarmanthanBackend`
  - `C:\xampp\htdocs\sagarmanthan`
- Do not run repository-changing commands (`git add`, `commit`, `stash`, `pull`, `push`, merges) in those two folders.
- If a request mentions either blocked folder, ask for explicit confirmation before taking any action there.
- If context from blocked folders is needed for comparison, keep access read-only and summarize findings without edits.
